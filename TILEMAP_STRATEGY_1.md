# Стратегия управления динамическими слоями тайлмапа

## Контекст и проблема

### Текущая архитектура

Файлы проекта:

- `src/game/scenes/MainScene.ts` - главная сцена игры
- `src/game/scenes/TilemapController.ts` - контроллер управления тайлмапом

### Описание проблемы

Создание нового слоя тайлмапа занимает 100-300мс, что является медленной операцией. Необходимо обеспечить плавную работу при движении камеры, учитывая следующие сложности:

1. **Проблема #1: Определение остановки камеры**
   - Нужно понять, когда камера остановилась, чтобы центрировать слой под ней
   - Проверка в каждом кадре (метод `update()`) создаёт избыточную нагрузку на рендер
   - Требуется эффективный способ детекции остановки без нагрузки на производительность

2. **Проблема #2: Предотвращение параллельного создания слоёв**
   - Создание слоя в будущем будет асинхронной операцией (оптимизация)
   - Могут возникнуть race conditions: одновременное создание слоя от движения камеры и от центрирования
   - Необходим механизм синхронизации и очереди операций

3. **Проблема #3: Сложное движение камеры**
   - Текущий подход: проверка положения камеры в зонах (левая/правая/верхняя/нижняя треть слоя)
   - Сценарий: камера движется влево до левой трети слоя → поворачивает вверх → остаётся в левой зоне
   - Результат: постоянные сигналы о движении влево, затем влево-вверх, но никогда не наступает событие "движение вверх"
   - Слои создаются не оптимально, система не адаптируется к изменению направления

### Текущая реализация (на момент создания документа)

#### MainScene.ts

```typescript
create() {
  // ...
  this.tilemapStreamingTimer = this.time.addEvent({
    delay: this.tilemapController.getAvgTileGenTime(),
    callback: async () => {
      await this.tilemapController.reloadLayerOnCameraShift();
      const delay = this.tilemapController.getAvgTileGenTime();

      this.tilemapStreamingTimer?.reset({
        delay,
        callback: this.tilemapStreamingTimer.callback,
      });
    },
  });
}
```

Проблемы:

- Таймер использует среднее время генерации, что может быть неоптимально
- Нет механизма центрирования слоя при остановке камеры
- Нет предиктивной загрузки на основе направления движения

#### TilemapController.ts

```typescript
reloadLayerOnCameraShift() {
  const shift = this.checkCameraPosition();
  if (!shift) return;
  // ... создание слоя
}

private checkCameraPosition() {
  // Проверяет нахождение в зонах (трети слоя)
  const thirdWidth = Math.round(widthTiles / 3);
  const thirdHeight = Math.round(heightTiles / 3);

  const x = centerTile.x < thirdWidth ? -1 : centerTile.x > widthTiles - thirdWidth ? 1 : 0;
  const y = centerTile.y < thirdHeight ? -1 : centerTile.y > heightTiles - thirdHeight ? 1 : 0;
}
```

Проблемы:

- Статический подход к зонам, не учитывает направление движения
- Нет отслеживания вектора скорости камеры
- Синхронная операция (не готова к асинхронности)

---

## Предложенное решение: Стратегия на основе вектора движения камеры

### Ключевые компоненты

#### 1. Трекер движения камеры (Camera Motion Tracker)

**Назначение:** Отслеживание направления и скорости движения камеры без нагрузки на рендер.

**Структура данных:**

```typescript
private cameraMotionTracker = {
  lastPosition: { x: 0, y: 0 },          // Последняя зафиксированная позиция
  lastVelocity: { x: 0, y: 0 },          // Сглаженный вектор скорости
  isMoving: false,                        // Флаг движения
};
```

**Принцип работы:**

- Проверка позиции камеры каждые 100мс (не в каждом кадре!)
- Вычисление мгновенного вектора движения: `dx = currentX - lastX`
- Сглаживание вектора с использованием экспоненциального фильтра:

  ```
  smoothedVelocity = lastVelocity * 0.7 + instantVelocity * 0.3
  ```

- Определение состояния движения по пороговому значению скорости

**Преимущества:**

- ✅ Нет нагрузки на каждый кадр рендера
- ✅ Сглаживание устраняет дрожание при незначительных колебаниях камеры
- ✅ Точное определение момента остановки

**Недостатки:**

- ⚠️ Задержка детекции ~100мс (приемлемо для данной задачи)
- ⚠️ Требует калибровки порогового значения скорости

#### 2. Система блокировок и очередь намерений (Mutex + Intent Queue)

**Назначение:** Предотвращение параллельного создания слоёв и обработка отложенных операций.

**Структура данных:**

```typescript
private layerCreationState = {
  isCreating: false,                     // Флаг блокировки
  pendingIntent: null as {               // Очередь намерений (размер 1)
    type: 'shift' | 'center',
    direction?: { x: number, y: number }
  } | null,
};
```

**Принцип работы:**

```typescript
async createLayerSafe(type: 'shift' | 'center', direction?: { x: number, y: number }) {
  // 1. Проверка блокировки
  if (this.layerCreationState.isCreating) {
    // Сохраняем новое намерение (перезаписываем старое)
    this.layerCreationState.pendingIntent = { type, direction };
    return;
  }

  // 2. Устанавливаем блокировку
  this.layerCreationState.isCreating = true;

  try {
    // 3. Выполняем операцию
    if (type === 'shift' && direction) {
      await this.reloadLayerOnCameraShift(direction);
    } else {
      await this.centerLayerOnCamera();
    }
  } finally {
    // 4. Снимаем блокировку
    this.layerCreationState.isCreating = false;

    // 5. Обрабатываем отложенное намерение
    if (this.layerCreationState.pendingIntent) {
      const intent = this.layerCreationState.pendingIntent;
      this.layerCreationState.pendingIntent = null;
      // Рекурсивный вызов для обработки
      this.createLayerSafe(intent.type, intent.direction);
    }
  }
}
```

**Важно:** Очередь имеет размер 1 - новое намерение перезаписывает старое. Это оптимально, т.к.:

- Самое актуальное намерение важнее старых
- Не накапливаются устаревшие операции
- Простая и предсказуемая логика

**Преимущества:**

- ✅ Гарантированно нет параллельного создания слоёв
- ✅ Не теряются намерения, пришедшие во время блокировки
- ✅ Автоматическая обработка очереди
- ✅ Готовность к асинхронным операциям

**Недостатки:**

- ⚠️ Очередь размером 1 может отбросить промежуточные намерения (это фича, не баг)

#### 3. Определение направления движения с приоритетом

**Назначение:** Определение доминирующего направления движения камеры для предиктивной загрузки.

**Алгоритм:**

```typescript
private trackCameraMotion(): { x: number, y: number } | null {
  const { centerX, centerY } = this.scene.cameras.main;
  const { lastPosition, lastVelocity } = this.cameraMotionTracker;

  // Шаг 1: Вычисляем мгновенный вектор
  const dx = centerX - lastPosition.x;
  const dy = centerY - lastPosition.y;

  // Шаг 2: Сглаживаем с предыдущим значением (экспоненциальный фильтр)
  const smoothingFactor = 0.7;
  const velocityX = lastVelocity.x * smoothingFactor + dx * (1 - smoothingFactor);
  const velocityY = lastVelocity.y * smoothingFactor + dy * (1 - smoothingFactor);

  // Шаг 3: Определяем направление по порогу
  const threshold = 5; // пикселей за 100мс
  const absX = Math.abs(velocityX);
  const absY = Math.abs(velocityY);

  let direction = null;

  if (absX > threshold || absY > threshold) {
    // Камера движется
    if (absX > absY * 1.5) {
      // Преимущественно горизонтальное движение
      direction = { x: Math.sign(velocityX), y: 0 };
    } else if (absY > absX * 1.5) {
      // Преимущественно вертикальное движение
      direction = { x: 0, y: Math.sign(velocityY) };
    } else {
      // Диагональное движение
      direction = { x: Math.sign(velocityX), y: Math.sign(velocityY) };
    }
    this.cameraMotionTracker.isMoving = true;
  } else {
    // Камера остановилась
    if (this.cameraMotionTracker.isMoving) {
      // Только что остановилась - планируем центрирование
      this.cameraMotionTracker.isMoving = false;
      this.scheduleCenterLayerOnCamera();
    }
  }

  // Шаг 4: Обновляем состояние трекера
  this.cameraMotionTracker.lastPosition = { x: centerX, y: centerY };
  this.cameraMotionTracker.lastVelocity = { x: velocityX, y: velocityY };

  return direction;
}
```

**Параметры для калибровки:**

- `smoothingFactor = 0.7` - вес предыдущего значения скорости (чем выше, тем плавнее, но медленнее реакция)
- `threshold = 5` - минимальная скорость для считывания движения (пикселей за 100мс)
- `1.5` - коэффициент приоритета оси (если X в 1.5 раза больше Y, считаем горизонтальным)

**Преимущества:**

- ✅ Адаптируется к изменению направления движения
- ✅ Различает горизонтальное, вертикальное и диагональное движение
- ✅ Устойчив к шуму и дрожанию
- ✅ Автоматически определяет остановку

**Недостатки:**

- ⚠️ Требует калибровки параметров под конкретную игру
- ⚠️ Может запаздывать при резкой смене направления (компромисс со сглаживанием)

#### 4. Предиктивная загрузка с адаптивными границами

**Назначение:** Создание нового слоя заранее, в направлении движения камеры.

**Алгоритм:**

```typescript
private shouldCreateNewLayer(direction: { x: number, y: number } | null): boolean {
  if (!direction) return false;

  const layer = this.getActiveLayer();
  const { centerX, centerY } = this.scene.cameras.main;
  const centerTile = layer.worldToTileXY(centerX, centerY);

  // Камера за пределами слоя - критично
  if (!centerTile) return true;

  const { width: widthTiles, height: heightTiles } = this.tilemap;

  // Две зоны границ:
  // 1. Обычная граница (1/3 слоя) - для неактивных направлений
  // 2. Расширенная граница (1/2 слоя) - для направления движения
  const edgeThreshold = Math.round(widthTiles / 3);
  const lookaheadThreshold = Math.round(widthTiles / 2);

  // Проверяем приближение к границе В НАПРАВЛЕНИИ ДВИЖЕНИЯ
  if (direction.x < 0 && centerTile.x < lookaheadThreshold) return true;
  if (direction.x > 0 && centerTile.x > widthTiles - lookaheadThreshold) return true;
  if (direction.y < 0 && centerTile.y < lookaheadThreshold) return true;
  if (direction.y > 0 && centerTile.y > heightTiles - lookaheadThreshold) return true;

  // Проверяем пересечение обычных границ (на всякий случай)
  if (centerTile.x < edgeThreshold || centerTile.x > widthTiles - edgeThreshold ||
      centerTile.y < edgeThreshold || centerTile.y > heightTiles - edgeThreshold) {
    return true;
  }

  return false;
}
```

**Ключевая идея:**

- В направлении движения используем более агрессивную границу (50% слоя)
- В других направлениях используем консервативную границу (33% слоя)
- Это решает проблему сложного движения: при повороте камеры вверх система сразу адаптируется

**Пример работы:**

```
Слой 100x100 тайлов
Камера в позиции (25, 50)
Движется влево (direction.x = -1)

Проверка:
- centerTile.x = 25
- lookaheadThreshold = 50
- 25 < 50 → ИСТИНА → создаём новый слой

Затем камера поворачивает вверх (direction.x = 0, direction.y = -1):
- Левая граница больше не проверяется агрессивно
- Проверяется верхняя граница: centerTile.y < 50
- Система корректно адаптируется
```

**Преимущества:**

- ✅ Слой создаётся заранее, до выхода камеры за границы
- ✅ Адаптивность к направлению движения
- ✅ Решает проблему сложного движения
- ✅ Безопасная fallback-проверка обычных границ

**Недостатки:**

- ⚠️ Может создавать слои чаще, чем необходимо (но это лучше, чем задержки)
- ⚠️ Границы 1/3 и 1/2 могут требовать калибровки

#### 5. Отложенное центрирование слоя

**Назначение:** Центрирование слоя под камерой после остановки движения.

**Алгоритм:**

```typescript
private centerLayerTimeout: ReturnType<typeof setTimeout> | null = null;

private scheduleCenterLayerOnCamera() {
  // Отменяем предыдущий таймер, если был
  if (this.centerLayerTimeout) {
    clearTimeout(this.centerLayerTimeout);
  }

  // Планируем центрирование через 400мс после остановки
  this.centerLayerTimeout = setTimeout(() => {
    this.createLayerSafe('center');
  }, 400);
}
```

**Логика:**

- При остановке камеры запускается таймер на 400мс
- Если камера снова начнёт движение, таймер отменяется
- Это предотвращает центрирование при кратковременных паузах
- После задержки вызывается `createLayerSafe`, который учитывает блокировки

**Преимущества:**

- ✅ Не мешает плавному движению
- ✅ Центрирование только при реальной остановке
- ✅ Интегрируется с системой блокировок

**Недостатки:**

- ⚠️ Задержка 400мс может быть заметна пользователю (но это компромисс)
- ⚠️ Требует калибровки времени задержки

---

## Полная архитектура решения

### Модификации TilemapController.ts

#### Новые поля класса

```typescript
export class TilemapController {
  // ... существующие поля ...

  // Система блокировок
  private layerCreationState = {
    isCreating: false,
    pendingIntent: null as { type: 'shift' | 'center', direction?: { x: number, y: number } } | null,
  };

  // Трекер движения камеры
  private cameraMotionTracker = {
    lastPosition: { x: 0, y: 0 },
    lastVelocity: { x: 0, y: 0 },
    isMoving: false,
  };

  // Таймеры
  private centerLayerTimeout: ReturnType<typeof setTimeout> | null = null;
  private motionCheckTimer?: Phaser.Time.TimerEvent;
}
```

#### Инициализация в конструкторе

```typescript
constructor(scene: Scene) {
  // ... существующий код ...

  // Инициализация начальной позиции трекера
  const { centerX, centerY } = this.scene.cameras.main;
  this.cameraMotionTracker.lastPosition = { x: centerX, y: centerY };

  // Запуск таймера отслеживания движения камеры
  this.motionCheckTimer = this.scene.time.addEvent({
    delay: 100, // проверка каждые 100мс
    callback: () => this.onMotionCheck(),
    loop: true,
  });
}
```

#### Новые методы

**1. Обработчик проверки движения:**

```typescript
private onMotionCheck(): void {
  const direction = this.trackCameraMotion();

  if (direction && this.shouldCreateNewLayer(direction)) {
    this.createLayerSafe('shift', direction);
  }
}
```

**2. Трекинг движения камеры:**

```typescript
private trackCameraMotion(): { x: number, y: number } | null {
  // Полная реализация выше в разделе "Определение направления движения"
}
```

**3. Проверка необходимости создания слоя:**

```typescript
private shouldCreateNewLayer(direction: { x: number, y: number } | null): boolean {
  // Полная реализация выше в разделе "Предиктивная загрузка"
}
```

**4. Безопасное создание слоя:**

```typescript
private async createLayerSafe(
  type: 'shift' | 'center',
  direction?: { x: number, y: number }
): Promise<void> {
  // Полная реализация выше в разделе "Система блокировок"
}
```

**5. Планирование центрирования:**

```typescript
private scheduleCenterLayerOnCamera(): void {
  // Полная реализация выше в разделе "Отложенное центрирование"
}
```

#### Модификации существующих методов

**1. reloadLayerOnCameraShift - теперь async и принимает direction:**

```typescript
private async reloadLayerOnCameraShift(direction: { x: number, y: number }): Promise<void> {
  const startTime = performance.now();

  const { width, height } = this.tilemap;
  const { left, right, top, bottom } = this.scene.cameras.main.worldView;

  // ИЗМЕНЕНИЕ: используем переданный direction вместо повторного вычисления
  const offsetTilesX =
    direction.x > 0
      ? Math.round(left / TILE_SIZE) - 2
      : direction.x < 0
        ? Math.round(right / TILE_SIZE) + 2 - width
        : Math.round(this.getActiveLayer().x / TILE_SIZE);
  const offsetTilesY =
    direction.y > 0
      ? Math.round(top / TILE_SIZE) - 2
      : direction.y < 0
        ? Math.round(bottom / TILE_SIZE) + 2 - height
        : Math.round(this.getActiveLayer().y / TILE_SIZE);

  // ИЗМЕНЕНИЕ: добавлен await
  await this.updateLayer({
    X: offsetTilesX * TILE_SIZE,
    Y: offsetTilesY * TILE_SIZE,
    tileLayerData: TilemapController.buildTileLayerData({
      widthTiles: width,
      heightTiles: height,
      offsetTilesX,
      offsetTilesY,
    }),
  });

  this.setAvgTileGenTime(performance.now() - startTime);
}
```

**2. centerLayerOnCamera - теперь async:**

```typescript
private async centerLayerOnCamera(): Promise<void> {
  const { X, Y, tileLayerData } = (() => {
    const { width: widthTiles, height: heightTiles } = this.tilemap;
    const { centerX, centerY } = this.scene.cameras.main;
    const X = Math.round((centerX - (widthTiles * TILE_SIZE) / 2) / TILE_SIZE);
    const Y = Math.round((centerY - (heightTiles * TILE_SIZE) / 2) / TILE_SIZE);

    const tileLayerData = TilemapController.buildTileLayerData({
      widthTiles,
      heightTiles,
      offsetTilesX: X,
      offsetTilesY: Y,
    });

    return { X, Y, tileLayerData };
  })();

  // ИЗМЕНЕНИЕ: добавлен await
  await this.updateLayer({ X, Y, tileLayerData });
}
```

**3. updateLayer - теперь async:**

```typescript
private async updateLayer({ X, Y, tileLayerData }: {
  X: number;
  Y: number;
  tileLayerData: number[][]
}): Promise<void> {
  // ВАЖНО: здесь можно добавить await для будущей асинхронной обработки
  // Например, генерация тайлов в Web Worker

  this.tileLayers[1]
    .setVisible(false)
    .setPosition(X, Y)
    .putTilesAt(tileLayerData, 0, 0)
    .setVisible(true);

  this.tileLayers.reverse();
  this.offsetTiles.X = X;
  this.offsetTiles.Y = Y;
  this.tileLayers[1].setVisible(false);
}
```

**4. УДАЛИТЬ метод checkCameraPosition:**

```typescript
// УДАЛИТЬ - больше не нужен, заменён на trackCameraMotion и shouldCreateNewLayer
private checkCameraPosition() { ... }
```

#### Очистка ресурсов

```typescript
// НОВЫЙ метод для очистки при уничтожении контроллера
public destroy(): void {
  if (this.motionCheckTimer) {
    this.motionCheckTimer.destroy();
  }
  if (this.centerLayerTimeout) {
    clearTimeout(this.centerLayerTimeout);
  }
}
```

### Модификации MainScene.ts

#### Изменения в методе create()

**УДАЛИТЬ старый код:**

```typescript
// УДАЛИТЬ этот блок:
this.tilemapStreamingTimer = this.time.addEvent({
  delay: this.tilemapController.getAvgTileGenTime(),
  callback: async () => {
    await this.tilemapController.reloadLayerOnCameraShift();
    const delay = this.tilemapController.getAvgTileGenTime();

    this.tilemapStreamingTimer?.reset({
      delay,
      callback: this.tilemapStreamingTimer.callback,
    });
  },
});
```

**ДОБАВИТЬ:**

```typescript
// Вся логика управления слоями теперь внутри TilemapController
// Ничего не добавляем - TilemapController инициализирует таймер в своём конструкторе
```

**Итого:** В `MainScene.create()` остаётся только:

```typescript
create() {
  this.tilemapController = new TilemapController(this);
  // TilemapController теперь полностью автономен

  // ... остальной код инициализации контроллеров ...
}
```

#### Удаление неиспользуемого поля

```typescript
// УДАЛИТЬ:
private tilemapStreamingTimer?: Time.TimerEvent;
```

#### Очистка при уничтожении (если есть метод destroy)

```typescript
destroy() {
  this.tilemapController?.destroy();
  super.destroy();
}
```

---

## Анализ решения

### ✅ Плюсы

1. **Производительность:**
   - Проверка движения только раз в 100мс, а не в каждом кадре
   - Нет избыточных вычислений в методе `update()`
   - Предиктивная загрузка предотвращает задержки

2. **Надёжность:**
   - Гарантированная защита от параллельных операций
   - Graceful handling конфликтующих намерений
   - Готовность к асинхронным операциям

3. **Адаптивность:**
   - Система реагирует на изменение направления движения
   - Учитывает сложные траектории камеры
   - Автоматическое центрирование при остановке

4. **Maintainability:**
   - Чистое разделение ответственности
   - TilemapController полностью автономен
   - Легко тестируемые компоненты

5. **Масштабируемость:**
   - Готовность к оптимизации через Web Workers
   - Параметры легко калибруются
   - Можно добавить дополнительные стратегии

### ⚠️ Минусы и компромиссы

1. **Задержка детекции движения:**
   - ~100мс между проверками
   - **Митигация:** для данной задачи это приемлемо, т.к. создание слоя занимает 100-300мс
   - **Альтернатива:** можно снизить до 50мс, но с небольшим увеличением нагрузки

2. **Очередь размером 1:**
   - Промежуточные намерения отбрасываются
   - **Обоснование:** актуальное намерение важнее старых
   - **Компромисс:** простота vs сохранение истории

3. **Возможные лишние создания слоёв:**
   - Агрессивные границы (50%) могут срабатывать чаще
   - **Митигация:** это лучше, чем задержки при пересечении границы
   - **Оптимизация:** можно добавить cooldown между созданиями

4. **Калибровка параметров:**
   - Требуется настройка под конкретную игру:
     - `smoothingFactor = 0.7` - баланс между плавностью и реакцией
     - `threshold = 5` - чувствительность к движению
     - `lookaheadThreshold = widthTiles / 2` - агрессивность предзагрузки
     - `centerLayerTimeout = 400` - задержка центрирования
   - **Решение:** можно сделать эти параметры настраиваемыми через config

5. **Использование setTimeout:**
   - Не связан с игровым циклом Phaser
   - **Потенциальная проблема:** может сработать в неподходящий момент
   - **Альтернатива:** использовать Phaser.Time.TimerEvent для `centerLayerTimeout`

6. **Инициализация позиции:**
   - Требуется корректная инициализация `lastPosition` в конструкторе
   - **Важно:** иначе первая проверка даст ложный вектор

### 🔧 Возможные улучшения

1. **Конфигурируемые параметры:**

```typescript
interface TilemapControllerConfig {
  motionCheckInterval?: number;      // default: 100
  smoothingFactor?: number;          // default: 0.7
  movementThreshold?: number;        // default: 5
  lookaheadRatio?: number;          // default: 0.5
  edgeRatio?: number;               // default: 0.33
  centerDelay?: number;             // default: 400
}

constructor(scene: Scene, config?: TilemapControllerConfig) {
  // ...
}
```

2. **Cooldown между созданиями:**

```typescript
private lastLayerCreationTime = 0;
private minLayerCreationInterval = 100; // мс

private async createLayerSafe(...) {
  const now = performance.now();
  if (now - this.lastLayerCreationTime < this.minLayerCreationInterval) {
    // Слишком частое создание - игнорируем
    return;
  }
  this.lastLayerCreationTime = now;
  // ...
}
```

3. **Метрики и отладка:**

```typescript
private metrics = {
  totalLayerCreations: 0,
  avgCreationTime: 0,
  rejectedIntents: 0,
  centerOperations: 0,
  shiftOperations: 0,
};

getMetrics() {
  return { ...this.metrics };
}
```

4. **Использование Phaser TimerEvent вместо setTimeout:**

```typescript
private scheduleCenterLayerOnCamera() {
  if (this.centerLayerTimer) {
    this.centerLayerTimer.destroy();
  }

  this.centerLayerTimer = this.scene.time.delayedCall(400, () => {
    this.createLayerSafe('center');
  });
}
```

---

## План имплементации

### Шаг 1: Подготовка

1. ✅ Изучить текущую реализацию
2. ✅ Создать документ со стратегией (этот файл)
3. ⬜ Создать резервную копию/коммит

### Шаг 2: Модификация TilemapController.ts

1. ⬜ Добавить новые поля класса
2. ⬜ Реализовать `trackCameraMotion()`
3. ⬜ Реализовать `shouldCreateNewLayer()`
4. ⬜ Реализовать `createLayerSafe()`
5. ⬜ Реализовать `scheduleCenterLayerOnCamera()`
6. ⬜ Реализовать `onMotionCheck()`
7. ⬜ Модифицировать конструктор (добавить инициализацию)
8. ⬜ Модифицировать `reloadLayerOnCameraShift()` (async + direction param)
9. ⬜ Модифицировать `centerLayerOnCamera()` (async)
10. ⬜ Модифицировать `updateLayer()` (async)
11. ⬜ Удалить метод `checkCameraPosition()`
12. ⬜ Добавить метод `destroy()`

### Шаг 3: Модификация MainScene.ts

1. ⬜ Удалить поле `tilemapStreamingTimer`
2. ⬜ Удалить код создания таймера из `create()`
3. ⬜ Добавить вызов `destroy()` если нужно

### Шаг 4: Тестирование

1. ⬜ Проверить базовое движение камеры
2. ⬜ Проверить остановку и центрирование
3. ⬜ Проверить сложные траектории (поворот направления)
4. ⬜ Проверить производительность (FPS)
5. ⬜ Проверить отсутствие race conditions

### Шаг 5: Калибровка

1. ⬜ Настроить `smoothingFactor`
2. ⬜ Настроить `threshold`
3. ⬜ Настроить `lookaheadThreshold` и `edgeThreshold`
4. ⬜ Настроить `centerDelay`

### Шаг 6: Оптимизация (опционально)

1. ⬜ Добавить конфигурируемые параметры
2. ⬜ Добавить cooldown
3. ⬜ Добавить метрики
4. ⬜ Заменить setTimeout на Phaser TimerEvent

---

## Параметры для калибровки

| Параметр                 | Текущее значение | Описание                               | Как влияет                        |
| ------------------------ | ---------------- | -------------------------------------- | --------------------------------- |
| `motionCheckInterval`    | 100 мс           | Интервал проверки движения камеры      | ↓ = быстрее реакция, ↑ нагрузка   |
| `smoothingFactor`        | 0.7              | Вес предыдущего значения скорости      | ↑ = плавнее, но медленнее реакция |
| `threshold`              | 5 пикс/100мс     | Минимальная скорость для движения      | ↓ = чувствительнее к движению     |
| `lookaheadThreshold`     | widthTiles / 2   | Агрессивная граница (50%)              | ↑ = раньше создаём слои           |
| `edgeThreshold`          | widthTiles / 3   | Обычная граница (33%)                  | ↓ = позже создаём слои            |
| `centerDelay`            | 400 мс           | Задержка центрирования после остановки | ↓ = быстрее центрирование         |
| `directionPriorityRatio` | 1.5              | Коэффициент приоритета оси             | ↑ = сложнее определить диагональ  |

---

## Контрольный список восстановления контекста

Если произошёл сбой и нужно восстановить работу:

1. ✅ Прочитать этот документ полностью
2. ✅ Изучить текущее состояние файлов:
   - `src/game/scenes/TilemapController.ts`
   - `src/game/scenes/MainScene.ts`
3. ✅ Определить, на каком шаге находится имплементация (см. "План имплементации")
4. ✅ Проверить наличие новых полей и методов в TilemapController
5. ✅ Убедиться, что все async методы корректно используются
6. ✅ Проверить, что удалён старый код из MainScene
7. ✅ Запустить игру и проверить базовую функциональность
8. ✅ При проблемах - сверить код с примерами из этого документа

---

## Ссылки на код

### Файлы проекта

- **TilemapController:** `src/game/scenes/TilemapController.ts`
- **MainScene:** `src/game/scenes/MainScene.ts`
- **Constants:** `src/game/constants.ts`
- **Types:** `src/types/level.ts`

### Ключевые методы (текущие)

- `TilemapController.constructor()` - строка 29
- `TilemapController.centerLayerOnCamera()` - строка 59
- `TilemapController.reloadLayerOnCameraShift()` - строка 83
- `TilemapController.checkCameraPosition()` - строка 115 (будет удалён)
- `TilemapController.updateLayer()` - строка 139
- `MainScene.create()` - строка 25

### Конфигурация

- `TILE_SIZE` - размер тайла в пикселях
- `CAMERA_CONFIG` - настройки камеры
- `MOVEMENT_CONFIG` - настройки управления
